# Madina Jumaly — CV

A personal CV/portfolio site.

## Development

    npm install
    npm run dev

## Build

    npm run build
    npm run preview

## Deploy

Deployed on Vercel. `vercel.json` provides the SPA rewrite so client-side
routes (e.g. `/inner`) survive a hard refresh.
